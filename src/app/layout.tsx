import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "FEXnet — ACEM Fellowship Revision 2026",
    template: "%s | FEXnet",
  },
  description:
    "Senior FACEM-led emergency medicine education. Free weekly teaching from Christchurch Hospital plus the 2026 Revision Vault for ACEM Fellowship candidates.",
  keywords: ["ACEM", "Fellowship", "Emergency Medicine", "FACEM", "Revision", "NZ", "Australia"],
  openGraph: {
    siteName: "FEXnet",
    locale: "en_NZ",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en-NZ"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
